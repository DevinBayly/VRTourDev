let progressBar = (height,width,margins)=> {
    let ob = {}
    // make it variable the number of stops
    ob.setData = (data)=> {

    }
    ob.make = (numberPoints)=> {
        // generate svg
        // create the rectangular bar
        // enter add as a group the number of circles
        // attach change events to 

    }
    return ob
}

window.onload = ()=> {
    progressBar.make()
}